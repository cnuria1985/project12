[tool.poetry]
name = "project12"
version = "0.1.0"
description = ""
authors = ["нурия клементьева <cnuria@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
